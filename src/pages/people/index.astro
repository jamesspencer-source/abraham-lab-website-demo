---
import BaseLayout from "../../layouts/BaseLayout.astro";
import { peopleData } from "../../data/people";
import { groupPeople, withBase } from "../../lib/content";

const groupedPeople = groupPeople(peopleData.currentMembers);
const leadership = groupedPeople.find((group) => group.label === "Leadership");
const remainingGroups = groupedPeople.filter((group) => group.label !== "Leadership");
---

<BaseLayout title="People" description="Current members and alumni of the Abraham Lab.">
  <section class="interior-hero interior-hero--people">
    <div class="shell interior-hero__grid">
      <div class="interior-hero__copy reveal">
        <p class="eyebrow">People</p>
        <h1>Faculty, fellows, students, and operations leadership of the Abraham Lab.</h1>
        <p>
          The current roster reflects the laboratory's public-facing membership. Profiles remain concise and
          factual, with additional scientific context available through publications, research pages, and public
          institutional sources.
        </p>
      </div>
      <div class="interior-hero__rail reveal">
        <div class="metadata-stack">
          <p class="metadata-label">Current roster</p>
          <ul class="clean-list metadata-list">
            {remainingGroups.map((group) => <li>{group.label}</li>)}
          </ul>
        </div>
      </div>
    </div>
  </section>

  {leadership && (
    <section class="section section--flagship">
      <div class="shell directory-lead">
        {leadership.entries.map((person) => (
          <article class="directory-lead__row reveal">
            <div class="directory-lead__media frame" style={person.imagePosition ? `--media-position: ${person.imagePosition};` : undefined}>
              <img src={withBase(person.image)} alt={person.imageAlt} loading="lazy" />
            </div>
            <div class="directory-lead__body">
              <p class="eyebrow">Leadership</p>
              <h2>{person.name}</h2>
              <p class="directory-lead__title">{person.title}</p>
              {person.note && <p>{person.note}</p>}
            </div>
          </article>
        ))}
      </div>
    </section>
  )}

  <section class="section section--utility">
    <div class="shell directory-groups">
      {remainingGroups.map((group) => (
        <section class="directory-group reveal">
          <div class="directory-group__header">
            <p class="eyebrow">Current members</p>
            <h2>{group.label}</h2>
          </div>

          <div class="directory-grid">
            {group.entries.map((person) => (
              <article class="directory-record">
                <div class="directory-record__media frame" style={person.imagePosition ? `--media-position: ${person.imagePosition};` : undefined}>
                  <img src={withBase(person.image)} alt={person.imageAlt} loading="lazy" />
                </div>
                <div class="directory-record__body">
                  <h3>{person.name}</h3>
                  <p class="directory-record__title">{person.title}</p>
                  {person.note && <p>{person.note}</p>}
                </div>
              </article>
            ))}
          </div>
        </section>
      ))}
    </div>
  </section>

  <section class="section section--utility alumni-band">
    <div class="shell page-section__header page-section__header--compact">
      <div class="reveal">
        <p class="eyebrow">Alumni</p>
        <h2>Past lab members.</h2>
      </div>
    </div>

    <div class="shell alumni-shell">
      {peopleData.alumni.map((group) => (
        <section class="alumni-group reveal">
          <h3>{group.label}</h3>
          <div class="alumni-list">
            {group.entries.map((entry) => (
              <div class="alumni-row">
                <p class="alumni-row__name">{entry.name}</p>
                <p class="alumni-row__destination">{entry.destination ?? ""}</p>
              </div>
            ))}
          </div>
        </section>
      ))}
    </div>
  </section>
</BaseLayout>
